program webservice;

uses
  System.StartUpCopy,
  FMX.Forms,
  ufrm_webservice in 'ufrm_webservice.pas' {Form1};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
